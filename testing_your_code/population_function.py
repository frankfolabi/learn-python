def population(city, country, population=''):
    """Function to desplay a City, Country and the population."""
    place = f"{city.title()}, {country.title()} - population {population}"
    return place
